.message
  .message__sent
    %p.message__sent__member
      = message.user.name
    %p.message__sent__up-time
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
      
  - if message.content.present?
    %p.message__comment
      = message.content
  = image_tag message.image.url, class: 'message__comment__image' if message.image.present?